apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: aimarketing
  labels:
    app: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      imagePullSecrets:
        - name: ecr-pull
      containers:
        - name: frontend
          \1:v0.1.8
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          envFrom:
            # Secret first, then ConfigMap — ConfigMap wins for NEXTAUTH_URL / URLs.
            - secretRef:
                name: aimarketing-secrets
            - configMapRef:
                name: aimarketing-config
          volumeMounts:
            - name: secrets-files
              mountPath: /etc/aimarketing/secrets
              readOnly: true
      volumes:
        - name: secrets-files
          secret:
            secretName: aimarketing-secrets
            optional: true
