apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: aimarketing
  labels:
    app: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      imagePullSecrets:
        - name: ecr-pull
      containers:
        - name: backend
          image: 788612735038.dkr.ecr.ap-south-1.amazonaws.com/aimarketing-backend:v0.1.86
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          envFrom:
            # Secret first, then ConfigMap — ConfigMap wins for PUBLIC_API_BASE / URLs.
            - secretRef:
                name: aimarketing-secrets
            - configMapRef:
                name: aimarketing-config
          volumeMounts:
            - name: secrets-files
              mountPath: /etc/aimarketing/secrets
              readOnly: true
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
      volumes:
        - name: secrets-files
          secret:
            secretName: aimarketing-secrets
            optional: true
